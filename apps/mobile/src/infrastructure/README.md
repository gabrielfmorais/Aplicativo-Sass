# infrastructure/

Implementations of application ports: `supabase/` (client, repositories, queries, errors), `notifications/` (LocalNotificationAdapter), `analytics/`, `storage/`, `clock/`.
Must not import `features/` or `app/`. Empty until SPEC-001.
