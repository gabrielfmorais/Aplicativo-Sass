# DECISION REGISTER

| Campo | Valor |
|---|---|
| Status | v0.22 — 2026-08-28 — SPEC-000/001/002/004/**005/006/007/008/009/014** implementadas/merged; **Fases 5–8 fechadas**; **D-74 ratificada**; **D-73 ratificada**; SPEC-003 folded (D-66); D-70/D-71/D-72 **ratificadas por decisão humana** (D-70 com correção: o gate depende do conteúdo, não do autor). Regras capilares V1 continuam `candidate` (D-67): **PUBLIC RELEASE bloqueado** até o domain sign-off (`candidate → validated`, D-26/OQ-REL). LEVEL 2 ativo; **§0.2 Master Autonomous Engineering Mode** ativo |
| — | v0.23 — 2026-08-29 — **SPEC-010 aprovada em duas partes por decisão humana (D-78/D-79)**: infraestrutura (core + banco + Edge) autorizada; paywall/SDK e primeira capacidade premium seguem bloqueados por OQ1/OQ2/OQ3 |
| — | v0.24 — 2026-08-30 — **SPEC-010 Parte 1 IMPLEMENTADA/merged** (D-78): PR-A core (#35), **PR-B banco** (#36 — `subscriptions`, `billing_events`, `apply_billing_event`, `has_entitlement`, `get_my_entitlements`, pgTAP, paridade catálogo↔SQL), **PR-C Edge** (#37 — `billing-webhook` HMAC+zod+ACL, `config.toml` `verify_jwt=false`). `billing_events` substitui `audit_log` (nunca construído) — ADR-011 amendada. Full audit pré-release (3 dimensões): **0 BLOCKER**; achados IMPORTANT em escopo corrigidos, o resto reportado. **Parte 2 (PR-D/PR-E) segue bloqueada** por OQ1/OQ2/OQ3 (D-79) e pela dependência `react-native-purchases` (§4). CLAUDE.md §0 pendente de sync (§4, gate humano) |
| — | v0.25 — 2026-08-30 — **SPEC-010 Parte 2 APROVADA (D-79 DECIDED)**: RevenueCat (OQ1), `plan_customization` (OQ3), preço depois (OQ2). Construção da camada provider-agnóstica de PR-D em curso; adapter nativo RevenueCat + IAP real DEFERRED (development build — constraint do dono — + conta/produtos = credencial). Funcionalidade `plan_customization` será SPEC própria (NG7). |
| — | v0.26 — 2026-08-31 — **D-80**: Expo Web habilitado como **visualizador de desenvolvimento** (não plataforma de produto), autorizado pelo dono na mesma conversa (§4 — dependência). Três adapters `.web.ts` (sessão em memória, fresh-install no-op, notificações fail-closed); `export:check`/CI seguem só iOS+Android. Runbook `docs/runbooks/WEB-DEV-PREVIEW.md`. |
| — | v0.27 — 2026-08-31 — **SPEC-015 APROVADA (D-81, agente §0.2)**: a 1ª capacidade premium é **dias da semana preferidos**, aplicada por uma **camada de placement pura fora do engine** (ADR-007 e o gate D-26 intactos), gated server-side por `has_entitlement('plan_customization')`. PR-1 (core) implementada. |
| — | v0.28 — 2026-08-31 — **SPEC-015 IMPLEMENTADA/merged** (D-81): PR-1 core #42, PR-2 banco #43, PR-3 Edge+app #44. A primeira capacidade premium está completa e gated no servidor; **Fase 9 fechada** menos o purchase flow (DEFERRED: dev build + conta RevenueCat). **D-82**: o fim do ciclo de 4 semanas deixa de ser beco sem saída — CTA de reavaliação na Hoje + intent `reassessment_due`. |
| — | v0.29 — 2026-08-31 — **D-83 monetização: FREEMIUM** (mensal + anual + trial de 7 dias, sem paywall obrigatório na entrada, FREE completo no core) · **D-84 auth**: OTP por email **não** está funcional (Magic Link no template padrão, sem custom SMTP) — OAuth é o caminho que funciona · autorização humana para o agente sincronizar `CLAUDE.md` e fortalecer `.github/workflows/` (§4 amendada). |
| — | v0.30 — 2026-08-31 — **D-85**: acesso DEV-only ao preview web (`signInWithPassword` real, anon key, 4 travas, RLS intacta) — **não é auth de produto**. **D-86**: Auth real (Google + Apple + OTP + callbacks + testes de fluxo) vira **requisito obrigatório de beta/release**, com as credenciais externas marcadas TRUE HUMAN GATE. D-84 corrigida por medição: o Google não estava habilitado no DEV. |
| — | v0.31 — 2026-08-31 — **D-87**: o projeto Supabase **DEV nunca foi provisionado** (0 de 10 tabelas) — causa real do *"Não foi possível carregar seu perfil"* depois do DEV sign-in. Guardrail `check:remote-schema`, diagnóstico visível em `__DEV__` e runbook de provisionamento entregues; **aplicar as migrations depende de credencial do dono (TRUE HUMAN GATE)**. |
| — | v0.32 — 2026-08-31 — **SPEC-016 Beta Experience aprovada (D-88)**: identidade visual própria (base quente, grafite, acento ameixa, cores semânticas por cuidado), design system em `apps/mobile/src/design/`, onboarding em etapas. Sem dependência nova, sem tocar regra/schema/autorização. |
| — | v0.33 — 2026-08-31 — **SPEC-016 fatia 2 (D-89)**: a Hoje ganha hierarquia — cartão de foco com a única ação primária da tela, week strip informativo, estado sempre em palavra. Nada acionável colapsado (SPEC-007 AC5). Auditoria corrigiu contraste abaixo do AA e escrita silenciosamente inerte. |
| — | v0.34 — 2026-08-31 — **D-87 RESOLVIDO** (banco DEV provisionado, `all 10 tables present` em `ayecidupmxmirwfzwtea`) e **D-90**: as Edge Functions nunca foram deployadas, que é a causa real da falha ao criar cronograma. Guardrail `check:remote-functions`, falha diagnosticável sob `__DEV__`, e a regra de que **validação no ambiente real é parte do DONE** (CLAUDE.md §0.1). |
| — | v0.35 — 2026-08-31 — **D-91** (Strategy A nunca sobreviveu a um deploy; bundle materializado + guardrail do grafo deployável) e **Fatia 2 da SPEC-016 DONE**, marcada só depois da jornada real no DEV: sign-in → onboarding → preview → criar cronograma → Hoje → concluir → check-in → reload → persistido. Edge Functions deployadas via workflow manual `deploy-dev-functions`. |
| — | v0.36 — 2026-08-31 — **D-92 (HUMANA): MASTER PRODUCT SCOPE.** 55 capabilities passam a ser COMMITTED em `docs/product/MASTER-PRODUCT-BACKLOG.md`, com estado obrigatório e sem "future idea". Sequenciamento é do agente; **IA obrigatoriamente por último** (sem infraestrutura antecipada) e **Community DEFERRED BY DEPENDENCY** sem sair do roadmap. Um único plano pago, mensal e anual idênticos. |
| — | v0.37 — 2026-08-31 — **D-93: `improve --full` de checkpoint de beta.** Zero BLOCKER. RLS com guarda genérica + fixture negativa, 10 tabelas conferidas, nenhum TODO/console/teste pulado. Corrigidas três contradições doc↔implementação (CLAUDE.md §0, SPEC-016 §20, MVP-ROADMAP). AC1 verificado por varredura. Gates de beta restantes são todos humanos. |
| — | v0.38 — 2026-08-31 — **SPEC-016 IMPLEMENTADA.** As cinco fatias entregues e validadas no DEV real; o produto funcional virou produto apresentável. As quatro OQ (fonte própria, ícones, dark mode, motion) seguem abertas com gatilho — nenhuma virou necessidade, o que era a aposta de NG3. Próximo pelo MASTER PRODUCT BACKLOG (D-92). |
| — | v0.39 — 2026-08-31 — **D-94 (HUMANA): MASTER PRODUCT BLUEPRINT.** Segunda fonte canônica, complementar ao backlog: o que cada capability **significa** funcionalmente — objetivo, fluxo, regras, limites e o que ela **não** pode fazer. Fixa *Free executa e registra / Premium interpreta*, o tier único, e as três proibições de honestidade. Não altera o escopo de D-92 nem a autonomia de sequenciamento. |
| — | v0.40 — 2026-09-01 — **D-95: `profiles` existe, com uma coluna.** D-63 adiou a tabela até haver requisito concreto; a pergunta do nome na SPEC-018 é esse requisito. Nasce com `display_name` e nada mais — `timezone`, `locale` e `onboarding_status` continuam não existindo. Primeiro dado pessoal declarado pela usuária: opcional, apagável, e a existência da linha registra que já perguntamos. |
| — | v0.41 — 2026-09-01 — **SPEC-019 (F20) e SPEC-020 (F23)** e **D-96**: `hair_events` — o Free **registra** que o cabelo mudou e **oferece** reavaliar; não interpreta, não aconselha e não diagnostica, que é exatamente o que mantém a capability fora do gate D-26. Escrita só por RPC (dia civil e idempotência são do servidor). **Não é DONE:** a migration ainda não foi aplicada no DEV. |
| — | v0.44 — 2026-09-02 — **D-100 (HUMANA): o Catálogo Huna de produtos reais, detalhado.** Busca de marcas e produtos reais com imagem de fonte autorizada e EAN; adicionar à prateleira, selecionar no Wash Day, preservar no histórico. **Manual obrigatório como fallback** — o catálogo facilita o cadastro, não o substitui. Nada inventado, nada de scraping improvisado, direito de uso para imagens, **Brasil primeiro**, e interface de **busca** e não de vitrine. |
| — | v0.43 — 2026-09-01 — **D-99: catálogo de produtos reais (`F32`) e scanner/EAN (`F33`) ganham linha própria no roadmap**, em vez de existirem só como dependência de `P18`. `DEFERRED BY DEPENDENCY`: o que trava é curadoria, direito de imagem e custo — não engenharia. Nenhum dos dois substitui o cadastro manual. |
| — | v0.42 — 2026-09-01 — **D-97 (HUMANA): autonomia restabelecida e delimitada.** Decisão técnica ou de produto **reversível**, sem custo/contrato/obrigação legal, sem produção irreversível, sem gate de domínio, e com uma alternativa objetivamente superior identificável, **não para no dono**: o agente investiga, modela, escolhe, registra, implementa, testa, valida no DEV e segue — reportando depois. **D-98 (HUMANA):** OQ2 da SPEC-022 (deslocar preservando intervalos, fim do ciclo como limite) e OQ1 da SPEC-017 (derivar do snapshot de origem, não persistir derivável). |
| Uso | Fonte de verdade sobre o estado de cada decisão. Nenhum item muda de status sem registro nesta tabela. Agentes nunca resolvem itens `HUMAN DECISION` por conta própria; sob `CLAUDE.md` §0.2 podem decidir o que é **pequeno, reversível e de baixo risco**, registrando o item como `DECIDED (agente, §0.2)` para ratificação humana (bloco B6). Ratificado vira `RATIFIED (humano, data)`. |

Legenda de custo de mudança tardia: **L** (horas) · **M** (dias) · **H** (semanas ou migração de dados).
Status: `APPROVED` · `DECIDED` (decisão humana com conteúdo específico) · `DEFERRED` · `OPEN`.

> Nota de integridade (2026-08-26): a aprovação B1 referiu-se ao "bloco D-01…D-15". O bloco READY TO ACCEPT da Foundation Review continha **D-01…D-11 e D-13…D-15**; **D-12** estava (e permanece) em DEFER. Os itens aprovados abaixo são exatamente os que constavam no bloco, sem alteração de conteúdo.

---

## A. APPROVED — bloco READY TO ACCEPT (aprovação humana B1, 2026-08-26)

| ID | Decisão | Opção adotada | Motivo | Custo tardio | Status |
|---|---|---|---|---|---|
| D-01 | Camadas pragmáticas + domínio em `packages/core` (ADR-001) | camadas pragmáticas | Protege engines da UI/IA sem boilerplate | M | **APPROVED** |
| D-02 | Repo único, 2 workspaces (ADR-003) | 2 workspaces | Fronteira física do domínio com custo mínimo | L | **APPROVED** |
| D-03 | RLS como autorização primária; RPC para transações; Edge só para engine/segredos (ADR-004) | Supabase nativo | Menos infra; RLS testável | H | **APPROVED** |
| D-04 | Fusão Calendar + Execution + Check-in em Care Tracking (ADR-006) | 1 contexto, 3 subpastas | Mesmo agregado | L | **APPROVED** |
| D-05 | `Instant` vs `LocalDate` + tz IANA no perfil; `new Date()` proibido (ADR-008) | — | Sem alternativa séria | M | **APPROVED** |
| D-06 | Engines puros, versionados, golden tests; preview local + persistência server-side (ADR-007) | ambos com mesma versão | P01 + P10 | M | **APPROVED** |
| D-07 | Intent/Channel/Delivery para notificações (ADR-009, forma) | — | Port permite trocar canal | L | **APPROVED** |
| D-08 | Catálogo tipado de eventos + port; PII proibida (ADR-010) | — | — | L | **APPROVED** |
| D-09 | Billing ≠ Subscription ≠ Entitlement; entitlements por capacidade (ADR-011, forma) | 3 camadas | flag `is_premium` é insegura | M | **APPROVED** |
| D-10 | Exclusão de conta via `account_deletion_requests` (sem `profiles.deleted_at`) | tabela de pedido | Fonte única; cancelável | L | **APPROVED** |
| D-11 | `hair_profiles.version` por trigger com advisory lock (INSERT direto) | trigger | Evita RPC trivial | L | **APPROVED — amendado por D-64 (2026-08-27):** snapshots imutáveis identificados por `id` estável, **sem** numeração sequencial/trigger/advisory lock no MVP |
| D-13 | Sem Storage, Realtime, uploads, usuários anônimos no MVP | — | Superfície menor | L | **APPROVED** |
| D-14 | `CLAUDE.md` + proibições + CODEOWNERS + PR template | — | Governança de agentes | L | **APPROVED** |
| D-15 | 5 skills iniciais, read-only exceto `/spec-create` | — | — | L | **APPROVED** |

## B. DECIDED — decisões humanas com conteúdo específico (2026-08-26)

| ID | Decisão | Conteúdo decidido | Efeito documental | Status |
|---|---|---|---|---|
| D-20 | Framework mobile | **Expo + React Native + TypeScript strict.** Estrutural: só muda com nova ADR que substitua ADR-002 | ADR-002 → Accepted | **DECIDED** |
| D-21 | Autenticação | **Apple + Google + Email OTP/passwordless.** Email+senha **não** é fluxo principal do MVP. SPEC de Identity deve cobrir: Sign in with Apple, Google, Email OTP, account linking, prevenção de duplicidade, colisão email/provider, expiração de OTP, rate limiting, deep link security, sessão, logout, account recovery | ADR-005 revisada (permanece Proposed até diff aprovado) | **DECIDED** |
| D-22 | Notificações | **Local no MVP.** Domínio não conhece Expo Notifications. Separação `NotificationIntent → NotificationScheduler → NotificationAdapter`; primeira implementação `LocalNotificationAdapter`; push remoto fora do MVP, adicionável sem alterar domínio | ADR-009 atualizada → Accepted | **DECIDED** |
| D-23 | Admin UI | **DEFER.** Não criar `apps/admin` por antecipação; só com necessidade operacional concreta. Arquitetura preserva possibilidade | ADR-003 → Accepted (sem mudança) | **DEFERRED** |
| D-24 | Billing provider | **DEFER PROVIDER.** Preservar Store → Billing Provider → Subscription State → Entitlements → Application; nenhum componente do domínio depende do provider. Escolha só antes da fase Subscription | ADR-011 → Accepted (forma); provider em SPEC-010 | **DEFERRED (provider)** |
| D-25 | Streaks | **DEFER.** Não persistir streak no MVP; sem tabelas/campos. Se necessário, derivar de fatos de execução | DATA-MODEL §6 | **DEFERRED** |
| D-26 | Regras capilares | **Engineering may design the engine; may NOT invent production hair-care rules.** `DiagnosticRulesV1`/`ScheduleRulesV1` formalizadas separadamente com `rule_id, version, description, inputs, output, rationale/source, validation_status ∈ {draft, awaiting_domain_review, validated, deprecated}`; só `validated` pode ser production-ready. Claude não apresenta suposições como conhecimento validado. Não bloqueia Foundation | ADR-007 amendment; CLAUDE.md §2 | **DECIDED** |
| D-27 | MCP policy | **Supabase MCP:** só projeto de desenvolvimento, read-only por padrão, `project-ref` explícito. Proibido: produção, SQL irrestrito, migrations automáticas, pausar projeto, comandos destrutivos, bypass do workflow de migrations. **Lovable:** remover/não usar. **GitHub:** git + `gh` CLI; sem MCP sem necessidade comprovada | MCP-POLICY §4 | **DECIDED** |
| D-28 | Cuidado atrasado | **Nunca alterar silenciosamente o cronograma.** Mostrar estado e pedir decisão: `[Fazer hoje] [Reagendar] [Pular]`. Sistema nunca desloca o plano sem ação explícita ou regra futura aprovada. Preservar histórico | DOMAIN-MAP §3.5; DATA-MODEL §3.6 | **DECIDED** |

## B2. DECIDED — revisão humana da SPEC-000 (2026-08-26)

| ID | Decisão | Conteúdo decidido | Efeito | Status |
|---|---|---|---|---|
| D-41 | SPEC-000 | **APPROVED FOR IMPLEMENTATION** — somente Engineering Foundation; nenhuma feature de produto | SPEC-000 → Approved → In Progress | **DECIDED** |
| D-42 | ADR-005 | Diff aprovado → **Accepted**; auth só na SPEC-001 | ADR-005 Accepted | **DECIDED** |
| D-43 | Node.js | **22.23.x** (pin `.node-version` = `22.23.2` + `engines`). Não usar 20; não migrar para 24/26 automaticamente; upgrade de major é mudança intencional. Repo deve falhar cedo/avisar claramente em major não suportado | `.node-version`, `package.json#engines`, `scripts/check-node.mjs`, CI `setup-node` | **DECIDED** |
| D-44 | Expo | **SDK 57 / React Native 0.86**, pinado (sem "latest" flutuante); pacotes Expo via `expo install`; sem override manual de versões gerenciadas sem justificativa | `apps/mobile/package.json` | **DECIDED** |
| D-45 | CODEOWNERS | `@gabrielfmorais` **confirmado** por inspeção (remote `github.com/gabrielfmorais/Aplicativo-Sass`, API GitHub: login `gabrielfmorais`, type `User`, owner do repo). Sem segundo revisor inventado | `.github/CODEOWNERS` mantido | **DECIDED** |
| D-46 | Estratégia de implementação | Branch única `foundation/spec-000` com commits atômicos por checkpoint; revisão completa antes do merge; nunca em `main` | — | **DECIDED** |
| D-47 | Dependências da SPEC-000 | Aprovadas como **candidatas**, não obrigatórias; cada instalação justificada (requisito, manutenção, compatibilidade Node 22 + Expo 57 + runtime, install scripts, supply chain) | SPEC-000 §19 evidência | **DECIDED** |
| D-48 | `core != utils` | `packages/core` só contém primitivas de domínio/application independentes de runtime com razão arquitetural explícita; sem fs, env, rede, UI, side effects | `packages/core/README.md`, lint | **DECIDED** |
| D-49 | Spike core↔Deno | Estratégia A → B só com motivo documentado → **STOP** e escalar se B trouxer complexidade/duplicação/hacks; nunca emendar ADR-007 silenciosamente | `CORE-RUNTIME-SPIKE.md` | **DECIDED** |
| D-50 | AC12 — teste de autossuficiência documental (SPEC-000) | **DEFER.** Blocking: **NO**. Motivo: decisão de workflow humana — preservar a sessão ativa do agente em vez de abrir sessão nova exclusiva para o teste. Não executado, não simulado, sem evidência registrada. Trigger: próxima sessão naturalmente nova de agente ou onboarding de novo agente/desenvolvedor. Decisão operacional, não arquitetural | SPEC-000 §25 AC12 | **DEFERRED (2026-08-26)** |
| D-33 | date-fns / date-fns-tz | **Não introduzidas.** `toLocalDate` implementado com `Intl.DateTimeFormat` (nativo em Node, Deno e Hermes), testado incluindo DST histórico. Uma lib de datas só entra via SPEC futura com justificativa | `packages/core/src/shared/time/local-date.ts` | **DECIDED (2026-08-26, implementação SPEC-000)** |
| D-40 | Bundling core → Edge | **Estratégia A**: Deno consome `packages/core/src` diretamente via `supabase/functions/deno.json` (imports `.ts` no core). Sem build. Verificação residual do bundling de deploy na SPEC-004 | `docs/architecture/CORE-RUNTIME-SPIKE.md` | **DECIDED (2026-08-26)** |

## B3. DECIDED — aprovação da SPEC-001 Identity & Authentication (2026-08-26)

| ID | Decisão | Conteúdo decidido | Efeito | Status |
|---|---|---|---|---|
| D-51 | SPEC-001 | **APPROVED** (v0.3) após revisão humana de arquitetura, necessidade e segurança. Provedores: Apple + Google + Email OTP (reafirma D-21/ADR-005). Implementação **não** iniciada; escopo mínimo: Supabase Auth + persistência segura de sessão + `account_deletion_requests` + RLS/grants/constraints | SPEC-001 → Approved; índice | **DECIDED** |
| D-52 | Perfil de aplicação | `profiles` **adiado para a SPEC-002**; a SPEC-001 **não** usa trigger de provisionamento em `auth.users` nem RPC `ensure_my_profile`; quando existir, o perfil nasce por comando idempotente na primeira sessão autenticada (Opção B) | ADR-005 A1, DATA-MODEL §3.1, DOMAIN-MAP §3.1, RLS matrix | **DECIDED** |
| D-53 | Sessão | Armazenamento seguro do runtime obrigatório; **criptografia própria proibida** sem requisito demonstrado; storage seguro indisponível → sessão não persistente (fail-safe); expiração = configuração do Supabase | SPEC-001 §10 | **DECIDED** |
| D-54 | Account linking | **Provider-managed verified identity linking** (Supabase Auth, mesmo email verificado) aceito; **application heuristic account merging** proibido; nenhuma inferência sobre Apple Private Relay | SPEC-001 §7/§12 | **DECIDED** |
| D-55 | Exclusão de conta | Registro mínimo do pedido (`account_deletion_requests`: `user_id`, `requested_at`) por acesso direto com grants mínimos + RLS + PK; **sem RPC wrapper**; exclusão efetiva de `auth.users` permanece privilegiada/server-owned. **Pendente (humano):** exclusão imediata vs grace period e, com ela, o comportamento da sessão após o pedido | SPEC-001 §8/§18; DATA-MODEL §3.15 | **DECIDED (política de purga OPEN)** |

## B4. DECIDED — decisões de produto da SPEC-002 Hair Profile & Onboarding (2026-08-27)

| ID | Decisão | Conteúdo decidido | Efeito | Status |
|---|---|---|---|---|
| D-62 | Inputs mínimos do perfil capilar (MVP) | **8 inputs de produto aprovados** (não são diagnóstico médico/dermatológico): `hair_pattern`, `strand_thickness`, `scalp_tendency`, `wash_frequency`, `chemical_treatments` (multi), `heat_usage`, `current_concerns` (multi), `primary_goal`. Valores/UX em SPEC-002 §6. **Sem 2A–4C**, sem densidade. Não adicionar dimensões sem requisito concreto. Regras de diagnóstico continuam fora (D-26). | SPEC-002 §6; DATA-MODEL §3.3 (atualizar na implementação) | **DECIDED** |
| D-63 | `profiles` na SPEC-002 | **REMOVE.** `profiles`, `ProfilePort`, `onboarding_status`, provisioning e trigger em `auth.users` **não** são implementados agora. Ownership direto `auth.users → hair_profiles.user_id`; "onboarding concluído" é derivado da existência de um hair profile válido. `profiles` nasce numa SPEC futura com requisito concreto. Confirma a necessity review; um DEFER anterior (D-52) não obriga a implementar. | SPEC-002 §12; DATA-MODEL §3.1 (atualizar na implementação) | **DECIDED** |
| D-64 | Versionamento de `hair_profiles` (amenda D-11) | **"Versioned profile" = snapshots históricos imutáveis identificados por `id` estável, não necessariamente por número sequencial.** REMOVE `version int`, trigger de alocação, `MAX+1`, `UNIQUE(user_id,version)` e a lógica de concorrência criada só para numerar. Cada avaliação = nova linha imutável; atual = mais recente (`created_at desc, id desc`); downstream referencia `hair_profile_id`. Verificado: nenhum requisito atual depende de ordinal sequencial. | **Amenda D-11**; SPEC-002 §8/§9; DATA-MODEL §3.3 + DOMAIN-MAP §3.2 (atualizar na implementação) | **DECIDED** |
| D-65 | SPEC-002 | **APPROVED** (v0.4) após revisão humana. Clarificações vinculantes: `chemical_treatments` `[]` = nenhuma (sem enum `none`); `current_concerns.no_major_concern` exclusivo (`cardinality=1`) por validação de cliente + CHECK server-side (sem RPC/trigger); **analytics DEFER** (nenhum evento/no-op na SPEC-002 — SPEC-011). Implementação autorizada (LEVEL 2). | SPEC-002 → Approved; índice | **DECIDED** |
| D-66 | SPEC-003 / fronteira Diagnostic×Schedule | **FOLD**: a entrega MVP do Assessment/Diagnostic Engine acontece na **SPEC-004** (vertical slice), não numa SPEC-003 isolada (único consumidor do resultado é o Schedule; taxonomia só definível junto ao Schedule; SPEC-003 isolada seria pacote-esqueleto). **Fronteira de domínio preservada** (módulos `diagnostic/`+`schedule/`). `diagnostic_results` **não** pré-aprovada (necessity review na SPEC-004). Falsa precisão (`confidence`/score) proibida; `evidence`/reason codes no core, copy na UI. | **ADR-007 Amendment A2**; SPEC-003 → Folded; SPEC-004 index | **DECIDED** |
| D-67 | SPEC-004 regras de domínio V1 | **CANDIDATE rules** aprovadas (heurísticas de produto cosméticas, não diagnóstico): care types H/N/R; `AssessmentOutput={emphasis,includeReconstruction,evidenceCodes}` (sem levels/score/confidence); emphasis por `primary_goal`→`current_concerns`→`hair_pattern`; reconstruction 2-de-3 (CHEMICAL/HIGH_HEAT/DAMAGE); sessions/week por `wash_frequency`; janela 28d; ciclos H/N/R; 1 R após dia 14; offsets determinísticos; unknown never escalates. **Esclarece D-26:** `candidate` PODE ser implementada/testada em dev/internal beta; **PUBLIC RELEASE** exige `validated` (domain sign-off) — requisito de release **não** enfraquecido. | **ADR-007 A1** (add `candidate`); `docs/domain-rules/SPEC-004-domain-rules-worksheet.md`; SPEC-004 §13 | **DECIDED (candidate)** |
| D-68 | SPEC-004 | **APPROVED** (v0.5) por aprovação humana. Escopo confirmado: Assessment + Schedule na mesma vertical slice com módulos internos separados; regras V1 `candidate` autorizadas para implementação/internal beta; **D-26 permanece PUBLIC RELEASE GATE**; care types `hydration`/`nutrition`/`reconstruction`; `AssessmentOutput` V1; emphasis rules; reconstruction rule; cadência; janela de 28 dias; política `unknown`/`varies`; evidence codes mínimos; modelo técnico v0.3/v0.4 fechado. Implementação autorizada (LEVEL 2). | SPEC-004 → Approved; índice de SPECs | **DECIDED** |

## B5. DECIDED — decisões de produto da SPEC-005 Care Tracking (2026-08-27)

| ID | Decisão | Conteúdo decidido | Efeito | Status |
|---|---|---|---|---|
| D-12 | Desfazer execução | **SIM.** A usuária pode anular uma execução registrada por engano dentro de **15 minutos** da criação. A execução anulada **permanece** no histórico (`voided_at`), nunca é apagada; o cuidado volta ao estado derivado de não concluído (desde que não tenha sido pulado/reagendado) e pode ser registrado de novo. **Fora do escopo:** edição arbitrária de histórico, correção de execução de dias anteriores, undo ilimitado, fluxo administrativo. | SPEC-005 §7 BR4b/BR4c, §9 `void_execution`, AC16/AC17 | **DECIDED** |
| D-35 | Múltiplas execuções por `scheduled_care` | **NÃO.** Invariante de produto: um `ScheduledCare` → **0 ou 1** `CareExecution` **efetiva**. Uma execução anulada por D-12 não conta, portanto após desfazer é possível registrar de novo. Garantido **no banco** (índice único parcial `WHERE voided_at IS NULL`), não só na UI. Sem suporte a duas execuções válidas nem a repetição do mesmo cuidado como feature; execução avulsa continua DEFER. | SPEC-005 §8.3, AC18 | **DECIDED** |
| D-69 | SPEC-005 | **APPROVED** (v0.2). Escopo confirmado: Hoje/atrasado/próximos/histórico + concluir, pular, reagendar e desfazer (15 min). `status='completed'` **não** persistido — conclusão derivada da existência de execução efetiva (evita duas fontes de verdade). Reusa integralmente D-28 e as regras já aprovadas de reagendar/pular/overdue. Implementação autorizada (LEVEL 2). | SPEC-005 → Approved; índice de SPECs | **DECIDED** |

## B6. DECIDED — SPEC-007 Content v1 (2026-08-28)

> **Origem:** decisões tomadas pelo **agente** sob `CLAUDE.md` §0.2 (Master Autonomous Engineering Mode), aplicando um precedente humano já existente (D-67). **Todas as três foram RATIFICADAS por decisão humana em 2026-08-28**, D-70 com uma correção (o gate depende do conteúdo, não do autor). Não reabrir.

| ID | Decisão | Conteúdo decidido | Efeito | Status |
|---|---|---|---|---|
| D-70 | Autoria do conteúdo capilar V1 (SPEC-007 OQ-1) | **Aplica-se o precedente D-67 ao texto.** A engenharia redige os guias V1 com `validationStatus: 'candidate'` e `rationaleSource` declarando "hipótese de engenharia — requer revisão especializada". Liberados para **dev/internal beta**; entram em **OQ-REL**, o mesmo sign-off de domínio que já bloqueia o PUBLIC RELEASE das regras V1. **Nenhum gate novo; o gate de PUBLIC RELEASE permanece inalterado** (D-26 / ADR-007 A1). O conteúdo é **procedimental e cosmético**: sem marca, sem produto comercial, sem dosagem química, sem promessa de resultado e sem linguagem de diagnóstico dermatológico — o tempo de pausa remete sempre à embalagem do produto da usuária, nunca a um número inventado pela engenharia. Reversível: é texto num único arquivo. **RATIFICADA por decisão humana (2026-08-28) com uma correção:** o gate depende do **conteúdo**, não de quem escreveu. (A) Copy puramente editorial/UX, sem nova orientação capilar, segue o processo editorial normal. (B) Texto com **orientação ou regra capilar substantiva** continua sujeito a D-26 e ao sign-off do revisor de domínio **mesmo quando fornecido pelo owner humano** — nenhuma origem de texto torna conteúdo `validated` automaticamente, e **OQ-REL não é enfraquecida**. | SPEC-007 §7 BR2/**BR2b**/BR3, §17 AC3/AC4; `packages/core/src/content/v1/guides.ts` | **RATIFIED (humano, 2026-08-28)** |
| D-71 | Armazenamento do conteúdo V1 (SPEC-007 OQ-2) | **Sem tabelas `care_types`/`content_articles` nesta fatia** (necessity review D-47/D-48; precedente direto: `diagnostic_results` removido na SPEC-004). O conteúdo vive no bundle (`packages/core/src/content/`): disponível offline, sem loading/erro/retry, sem policy ou grant novos. **Gatilho nomeado para criar as tabelas** — o primeiro que ocorrer: (1) conteúdo precisar mudar sem release do app; (2) gating premium entrar em escopo (SPEC-010); (3) existir editor/admin de conteúdo; (4) segundo idioma. A migração é aditiva: `CareGuide` já é o formato de leitura. **RATIFICADA por decisão humana (2026-08-28)**: para a V1, manter o conteúdo no bundle; não criar tabela, CMS, conteúdo remoto nem infraestrutura de publicação dinâmica agora. | SPEC-007 §8.2; DATA-MODEL §3.9/§3.10 (nota de adiamento) | **RATIFIED (humano, 2026-08-28)** |
| D-72 | SPEC-007 | **APPROVED** (v0.2) sob §0.2, uma vez que D-70 e D-71 removem a única questão BLOCKING. Escopo: guias "como fazer" por care type na tela Hoje. Sem tabela, sem RPC, sem migration, sem dependência, sem analytics. Implementação autorizada (LEVEL 2). **RATIFICADA por decisão humana (2026-08-28)**, junto com a autorização de auto-merge das PRs da SPEC-007. | SPEC-007 → Approved; índice de SPECs | **RATIFIED (humano, 2026-08-28)** |

## B7. DECIDED — SPEC-006 Check-ins (2026-08-28)

> Decisão do agente sob `CLAUDE.md` §0.2, **RATIFICADA por decisão humana em 2026-08-28**. Nenhuma regra capilar nova: perguntar "como ficou?" e guardar a resposta não afirma nada sobre cabelo, então **D-26 não é acionada**.

| ID | Decisão | Conteúdo decidido | Efeito | Status |
|---|---|---|---|---|
| D-73 | SPEC-006 | **APPROVED** (v0.1). Escopo: **uma** pergunta ("Como ficou?", escala 1..5 já aprovada em DATA-MODEL §3.8), ancorada na **execução efetiva**, opcional, com retorno imediato na própria tela Hoje. Necessity review: as outras quatro dimensões (`hydration_feel`, `softness`, `definition`, `dryness`) e o texto livre `note` ficam **DEFER** — as primeiras existem para o Progress (Fase 8) e cada pergunta extra é atrito direto contra a hipótese H4; `note` é PII sem consumidor. Colunas anuláveis são aditivas, então a SPEC-009 acrescenta o que precisar sem migração de dados. Editar/apagar check-in: DEFER. Implementação autorizada (LEVEL 2). | SPEC-006 → Approved; DATA-MODEL §3.8; índice de SPECs | **RATIFIED (humano, 2026-08-28)** |

## B8. DECIDED — SPEC-008 Notifications (2026-08-28)

> Decisão do agente sob `CLAUDE.md` §0.2, dentro de ADR-009 (Accepted). **D-74 RATIFICADA por decisão humana em 2026-08-28**: os 3 intents ficam como estão; `reassessment_due` fora enquanto não existir fluxo real de reavaliação; `habit_recovery` fora enquanto não houver evidência de valor que justifique o risco de irritação; `max_per_day = 2` permanece constante no core; **não criar coluna/configuração no banco sem necessidade atual** — reabrir só por migration aditiva se surgir requisito de personalização. Não reabrir a SPEC-008. A **dependência `expo-notifications` foi aprovada por decisão humana** em 2026-08-28 (CLAUDE.md §4). Nenhuma regra capilar: lembrar não afirma nada sobre cabelo — D-26 não acionada.

| ID | Decisão | Conteúdo decidido | Efeito | Status |
|---|---|---|---|---|
| D-74 | SPEC-008 | **APPROVED** (v0.1). Canal **local** (D-22), opt-in duplo (preferência + permissão do SO). Necessity review sobre ADR-009: **3 intents** — `care_today`, `care_overdue`, `checkin_pending`. `reassessment_due` é **impossível** hoje (não existe reavaliação — SPEC-014) e `habit_recovery` fica **DEFER** (dispara justamente quando ela não está usando o app, maior risco de irritação, sem dado que justifique). `max_per_day` **removida** como coluna: nenhuma UI a altera, então a regra vive como constante `MAX_NOTIFICATIONS_PER_DAY = 2` no core. Sem `notification_deliveries`, sem `device_tokens`, sem tabela de intents agendados, sem rota de deep link parametrizada (evita a superfície T10). Horizonte de 14 dias. Implementação autorizada (LEVEL 2). | SPEC-008 → Approved; DATA-MODEL §3.11; DOMAIN-MAP §3.7 | **RATIFIED (humano, 2026-08-28)** |
| D-75 | Dependência `expo-notifications` | **APROVADA por decisão humana** (2026-08-28). Instalada por `expo install` (D-44) → `~57.0.15`, gerenciada pelo SDK 57. `pnpm audit --audit-level=high` continua em exit 0; o único advisory `moderate` (`uuid`) é **pré-existente**, não introduzido por esta instalação. Config plugin registrado em `app.json`. | `apps/mobile/package.json`, `app.json` | **DECIDED (humano)** |

## B9. DECIDED — SPEC-009 Progress v1 (2026-08-28)

> Decisão do agente sob `CLAUDE.md` §0.2, dentro do que DOMAIN-MAP §3.6 já previa. **Nenhuma afirmação capilar é feita** — só contagem e média aritmética do que ela registrou — então **D-26 não é acionada**.

| ID | Decisão | Conteúdo decidido | Efeito | Status |
|---|---|---|---|---|
| D-76 | SPEC-009 | **APPROVED** (v0.1). Escopo: **três fatos derivados** — quantos cuidados já foram decididos e quantos ela concluiu (fração exata, **nunca porcentagem**), quantos pulou, e como tem avaliado (média **auto-relatada**, retida abaixo de 3 respostas). **Zero persistência**: sem tabela, view, agregado, cache, RPC ou dependência — tudo derivado do `TodayView` que a tela já carrega, evitando segunda fonte de verdade (D-69). **Fora:** score de saúde capilar, % de melhora, claim causal, tendência/comparação entre períodos (com ≤28 dias é ruído apresentado como medida), gráficos, streaks (D-25), tela própria. Recorte no **plano ativo**, dito na tela; cruzar planos superseded é DEFER com gatilho nomeado. Implementação autorizada (LEVEL 2). | SPEC-009 → Approved; DOMAIN-MAP §3.6; índice de SPECs | **DECIDED (agente, §0.2)** |

## B10. DECIDED — SPEC-014 Reavaliação (2026-08-28)

> Decisão do agente sob `CLAUDE.md` §0.2. Reavaliação já está no escopo aprovado do MVP (PRODUCT-BRIEF §9.11) e **nenhuma regra capilar nova é criada** — os engines V1 `candidate` são invocados com entrada nova, não alterados. **D-26 não é acionada; o gate de PUBLIC RELEASE não se move.**

| ID | Decisão | Conteúdo decidido | Efeito | Status |
|---|---|---|---|---|
| D-77 | SPEC-014 | **APPROVED** (v0.1). Reavaliar é **fluxo de cliente**: a máquina de servidor já existia (`hair_profiles` append-only por D-64; `create_plan_tx` já faz supersede atômico; `generate-plan` já lê o snapshot mais recente). **Supersede só na confirmação** — salvar respostas novas não derruba o cronograma atual, então desistir no meio deixa tudo intacto; um snapshot sem plano é histórico legítimo (D-64), não lixo. **Zero migration, zero RPC, zero tela nova** (reusa `OnboardingScreen` e `PlanScreen`). Resolve o **OQ-2 da SPEC-009**: o total vitalício de execuções efetivas passa a aparecer quando supera o do plano atual, porque sem ele a primeira reavaliação faria o trabalho dela sumir de vista. **Fora:** nova versão de engine, `reassessment_due`, navegar planos passados, comparar avaliações ("o que mudou" seria inferência — D-26), cooldown. | SPEC-014 → Approved; SPEC-009 OQ-2 resolvida; DOMAIN-MAP §3.3/§3.6 | **DECIDED (agente, §0.2)** |

## B11. DECIDED — SPEC-010 Subscription & Entitlements, Parte 1 (2026-08-29)

> **Decisão humana** (2026-08-29): "aprovado parte 1". A SPEC-010 foi aprovada **em duas partes**, conforme a recomendação da revisão `spec-review`. A Parte 1 não depende de nenhuma das OQ BLOCKING, não gasta dinheiro e não exige credencial externa.

| ID | Decisão | Conteúdo decidido | Efeito | Status |
|---|---|---|---|---|
| D-78 | SPEC-010 — Parte 1 (infraestrutura) | **APPROVED** (v0.2), escopo **PR-A (core) + PR-B (banco) + PR-C (Edge)**: `EntitlementService` + `catalog.ts` puros; tabelas `subscriptions` (1:1, SELECT own, escrita de cliente negada por privilégio) e `billing_events` (idempotência por `event_id`, sem payload cru); RPC `apply_billing_event` (DEFINER, EXECUTE só `service_role`, allowlistada, `search_path` pinado — padrão `create_plan_tx`); `has_entitlement`/`get_my_entitlements` (STABLE, INVOKER); Edge `billing-webhook` (HMAC + zod + uma chamada à RPC), testada com fixtures genéricas e **sem deploy**. Amenda a ADR-011 (`billing_events` no lugar de `audit_log`, que não existe) e o catálogo da ADR-010 (`paywall_dismissed`, `subscription_restored`). Implementação autorizada (LEVEL 2). | SPEC-010 → Approved (Parte 1); ADR-011/ADR-010 amendadas; DATA-MODEL §3.12 | **DECIDED (humano, 2026-08-29)** |
| D-79 | SPEC-010 — Parte 2 (app) | **APROVADA (humano, 2026-08-30).** OQ1 = **RevenueCat**; OQ3 = **`plan_customization`**; OQ2 = **preço definido depois** (loja em runtime). Construção da camada **provider-agnóstica** autorizada e em curso (leitura de entitlements via `get_my_entitlements`, `PurchasesPort`, paywall + funil, RNTL). **DEFERRED (constraint do dono / credencial):** adapter nativo RevenueCat (`react-native-purchases`, exige development build) e IAP real (conta + produtos RevenueCat/App Store Connect/Google Play). A **funcionalidade** `plan_customization` é SPEC própria (NG7); esta entrega o gate reutilizável + paywall. | SPEC-010 §20/§23; OQ1/OQ3 resolvidas, OQ2 diferida | **DECIDED (humano, 2026-08-30)** |

| D-80 | Expo Web como visualizador de desenvolvimento | **APROVADO (humano, 2026-08-31 — pedido explícito na conversa; §4 exige autorização para dependência).** Web é **ambiente visual de dev**, nunca plataforma de produto: serve para acompanhar telas, onboarding, Hoje, Progresso, conta e paywall em viewport de celular com Fast Refresh. Dependências `react-native-web`, `react-dom`, `@expo/metro-runtime` entram em **`devDependencies`** (o manifesto diz que é ferramenta de dev). Três builds `.web.ts` por resolução de plataforma do Metro — **o bundle nativo não muda**: (1) sessão **em memória** (o navegador não tem keychain; `localStorage` exporia o refresh token a XSS — mantém a regra de D-53/D-59 de nunca cair para storage inseguro; recarregar a página desloga); (2) fresh-install **no-op** (aba não tem ciclo de instalação e a sessão já é efêmera); (3) notificações `ensurePermission → false` + `reconcile` no-op (aba não agenda lembrete local por data — reporta o mesmo estado de SPEC-008 EC2, fail closed, em vez de prometer o que não dispara). **Sem mock de regra de negócio, sem `Platform.OS` fora de adapter, sem web no CI, export, deploy ou store.** | `apps/mobile/package.json`, `app.json` (bloco `web`), 3 arquivos `.web.ts`, `docs/runbooks/WEB-DEV-PREVIEW.md` | **DECIDED (humano, 2026-08-31)** |

| D-81 | SPEC-015 — Plan Customization (1ª capacidade premium) | **APPROVED v0.2** com escopo mínimo. **OQ1 = dias da semana preferidos** (maior valor percebido, fora do domínio; "adiar início" já existe de graça via `startsOn`, "horário do dia" é lembrete e pertence à SPEC-008). **OQ2 = parâmetro puro na geração**: `buildPlan(snapshot, startsOn, preferences?)` aplica o placement **depois** do engine — plano ativo nunca é editado in place, troca só por supersede (SPEC-014). **Nenhum ADR novo** (agregado `HairPlan` imutável, NG2 preservado). **OQ3 = camada de placement fora de `engine/v1/`**, que não muda um byte: sem nova versão de engine (ADR-007), **o gate de PUBLIC RELEASE não se move** (D-26/D-67). **OQ4 = melhor esforço com aviso honesto**: cada cuidado vai para o dia preferido livre mais próximo; sem dia livre dentro da janela e a partir do início, **mantém a data do engine** — nada é removido nem "afinado" (reduzir cuidados seria mudar domínio); `fullyHonoured: false` informa o app. **OQ5 = persistir `plan_preferences` 1:1** (padrão `notification_preferences`): guardar a preferência **não** concede a capacidade — aplicá-la é gated server-side por `has_entitlement('plan_customization')` na geração (fail closed). Invariantes testadas: mesma contagem/tipos/ordem de cuidados, datas distintas, crescentes, dentro da janela, nunca antes do início. **Nada aqui é TRUE HUMAN GATE:** o dono já escolheu a capacidade premium (D-79); a alavanca dentro dela é decisão pequena, reversível e de baixo risco (§0.2), sem custo, credencial, publicação irreversível ou ciência capilar nova. | SPEC-015 → Approved; SPEC-004 `buildPlan` ganha parâmetro opcional; DATA-MODEL ganha `plan_preferences`; ADR-007/D-26 intactos | **DECIDED (agente, §0.2)** |

| D-82 | Fim do ciclo de 4 semanas (SPEC-005/008/014) | **DECIDED.** Buraco real encontrado na auditoria da SPEC-015: quando a janela de 28 dias acaba, a tela dizia "Seu cronograma chegou ao fim." **e não oferecia nada** — o produto simplesmente emudecia depois de quatro semanas, apesar de a reavaliação (SPEC-014) já existir na conta. Duas correções, ambas derivadas do que a tela já carrega, **zero persistência, zero migration, zero dependência**: (1) **Tela** — quando não há atrasado, nem hoje, nem próximo, a Hoje passa a oferecer "Reavaliar e montar o próximo", dizendo que o histórico continua salvo; some assim que ela tem algo a fazer, para não virar ruído no loop diário. (2) **Intent `reassessment_due`** (adiado na SPEC-008 por falta da regra de "quando lembrar") — a regra é **o dia seguinte ao último dia do plano**, ancorado no `plannedDate` **de todos** os cuidados, não no que ainda está acionável: ancorar no acionável moveria o lembrete a cada cuidado concluído e o **apagaria justamente quando ela terminasse o último**. Âncora fixa ⇒ dispara **uma vez** e se auto-limita (passado o dia, o intent deixa de ser produzido — sem nag diário); um plano novo re-ancora sozinho. Prioridade **mais baixa** do catálogo: cede a vaga do cap de 2/dia para atrasado e cuidado de hoje. Texto sem PII, sem alegação capilar. **Decisão pequena, reversível, de baixo risco (§0.2)** — nenhuma regra de domínio nova (D-26 intacto), nenhum ADR. | SPEC-008 §9.1 (`reassessment_due` sai de adiado); SPEC-005 §14 estado vazio; `NOTIFICATION_INTENT_TYPES` ganha um membro | **DECIDED (agente, §0.2)** |

| D-83 | Monetização: FREEMIUM, planos e paywall | **DECIDIDO (humano, 2026-08-31).** Modelo **freemium**: o FREE é um app **realmente útil e completo no core**, não uma demonstração; o PREMIUM vende **personalização, inteligência e conveniência avançadas**. **Sem paywall obrigatório na entrada** — nenhuma tela de bloqueio antes de a usuária receber valor. Planos **mensal** e **anual**, com **trial de 7 dias**. `plan_customization` (SPEC-015) permanece a primeira capability premium. **Preço continua diferido** (OQ2 da SPEC-010): valor e moeda vêm da loja em runtime, nunca hard-coded — o que foi decidido é a **estrutura**, não o número. Efeito no que já existe: **nenhuma migration**. A SPEC-010 já modela um tier com `product_code` por produto IAP e `status` incluindo `trial`, então mensal e anual são dois produtos do **mesmo** tier e o catálogo por capacidade não muda (BR2). A NG2 ("um plano no MVP") passa a ler-se **um tier, dois períodos de cobrança**. Consequência operacional: mover capacidade já existente do FREE para o PREMIUM é **mudança material na proposta de valor** ⇒ human gate (§0.1). | CLAUDE.md §0; SPEC-010 NG2/OQ2 amendadas; SPEC-015 inalterada | **DECIDED (humano, 2026-08-31)** |
| D-84 | Auth: o login por email não está funcional | **DECIDIDO (humano, 2026-08-31).** O Supabase DEV envia **Magic Link** pelo template padrão (`{{ .ConfirmationURL }}`); a tela da SPEC-001 pede um **código de 6 dígitos** (`{{ .Token }}`), e **custom SMTP não foi configurado**. Logo o código nunca chega: **OTP por email não pode ser documentado, testado ou relatado como funcional** até o template mudar. **Nada é "consertado" no cliente** — as duas saídas são de configuração (template com `{{ .Token }}`, ou custom SMTP com template próprio), e nenhuma delas é código. **Corrigido em 2026-08-31, medido em `/auth/v1/settings` do projeto DEV** (uma versão anterior desta linha supunha que OAuth resolveria; supunha errado): **o provider Google não está habilitado** (`"google": false`), a confirmação de email é **obrigatória** (`mailer_autoconfirm: false`) e o email embutido responde **`429 over_email_send_rate_limit`**. Ou seja: **nenhum** fluxo de login funciona hoje no projeto DEV sem configuração do dono — nem Google, nem código por email, nem auto-cadastro. Para o preview, D-85. **Recusado explicitamente:** persistir o *code verifier* do PKCE no navegador para fazer o Magic Link fechar no web — seria trocar segurança por conveniência de desenvolvimento. Auth, RLS e produção ficam intactos. | CLAUDE.md §0; `docs/runbooks/WEB-DEV-PREVIEW.md` §6; SPEC-001 §evidência | **DECIDED (humano, 2026-08-31)** |

| D-85 | Acesso DEV-only ao preview web | **DECIDIDO (agente, §0.2, a pedido do dono, 2026-08-31).** O preview não passava da tela de login: a UI pede código de 6 dígitos e o projeto DEV manda Magic Link. Solução: um botão **somente-dev** que faz `signInWithPassword` com a **anon key** contra o **Supabase Auth real** — sessão real, `auth.uid()` real, **RLS valendo exatamente como em produção**. Nada mockado, nada contornado, **nenhum `service_role` e nenhum segredo nosso**: a credencial é um usuário descartável do projeto DEV do próprio dono, criado uma vez no painel com *Auto Confirm*. **Quatro travas independentes**, cada uma suficiente sozinha: `__DEV__` (eliminado do bundle em release) · `Platform.OS === 'web'` (web não é plataforma de produto, D-80) · `EXPO_PUBLIC_APP_ENV === 'development'` · **as duas variáveis de credencial precisam existir**, e vivem só no `.env.local` (gitignored, ausente de CI e de build EAS) — esta última segura mesmo se as outras caíssem. 12 testes cobrem as travas. **Fluxos oficiais intocados:** componente próprio renderizado **ao lado** de `SignInScreen`, nunca dentro. **Corrigido durante a implementação:** a primeira versão criava o usuário via `signUp` — errado duas vezes, porque `signUp` dispara email de confirmação (dependendo justamente do subsistema quebrado, que responde `429`) e porque `mailer_autoconfirm: false` deixaria o usuário inconfirmável. Passou a ser **só sign-in**, com erro que nomeia o passo exato do painel. **Escopo, explicitamente: desbloquear a visualização do dono. Não é Auth de produto e não conclui nada (ver D-86).** | `dev-sign-in.ts`, `DevSignIn.tsx`, composition root, rota, `.env.example`; runbook §5 | **DECIDED (agente, §0.2)** |
| D-86 | Auth real é requisito obrigatório de beta/release | **DECIDIDO (humano, 2026-08-31).** O acesso DEV (D-85) **não substitui nem resolve** o Auth oficial, e **Auth de produção nunca pode ser dado como concluído porque o DEV sign-in funciona**. Antes de qualquer beta ou publicação, todos os itens abaixo precisam estar funcionando **de verdade e testados nos fluxos reais**: (1) **Google OAuth** real; (2) **Apple Sign In** real; (3) **Email OTP** entregando o **código que a UI espera** — hoje o template manda Magic Link, então ou o template passa a emitir `{{ .Token }}`, ou a UI muda para consumir link, e a decisão é de produto; (4) **redirects/callbacks corretos** por plataforma (esquema `haircare://`, universal/app links, allowlist do Supabase); (5) **testes reais dos fluxos**, não só unitários de adapter. **TRUE HUMAN GATE** nas partes externas: console do Google, conta Apple Developer, custom SMTP ou provider de email, e as allowlists de redirect. O agente segue com todo o resto do roadmap sem esperar por elas, e **reporta este item como pendente em qualquer close-out de beta/release**. | CLAUDE.md §0 (gates de release); SPEC-001 ganha pendência de release; bloqueia beta junto com D-26 | **DECIDED (humano, 2026-08-31)** |

| D-87 | Banco do projeto DEV nunca foi provisionado | **RESOLVIDO — ver estado atual abaixo.** *(Diagnóstico de 2026-08-31, mantido como histórico.)* Depois que o DEV sign-in (D-85) passou a funcionar, o app parava em *"Não foi possível carregar seu perfil"*. **Não era Auth, RLS nem grants: o projeto DEV remoto não tinha nenhuma das 10 tabelas** — `PGRST205` em todas. Cada migration só havia rodado em local e no CI; `pnpm verify` verde, pgTAP verde, e nada testava se o banco remoto existira alguma vez. **A tela de erro estava certa:** falha de leitura ≠ ausência de perfil, e tratar falha como "sem perfil" empurraria a usuária para o onboarding arriscando um segundo snapshot (SPEC-002). Ausência é `null` → onboarding, e isso já funcionava. **Entregue:** (a) `scripts/check-remote-schema.mjs` — compara as tabelas que as migrations criam com o que o PostgREST expõe, **sem credencial**, e deriva a lista das próprias migrations para não haver drift; (b) a tela de erro passa a mostrar o motivo real **só sob `__DEV__`**; (c) runbook `DEV-DATABASE-PROVISION.md`. ✅ **ESTADO ATUAL (medido 2026-08-31, depois do provisionamento manual pelo dono): `hair-care-dev` / ref `ayecidupmxmirwfzwtea` — `check-remote-schema` responde `OK — all 10 tables present`.** DEV sign-in, onboarding real e o preview do cronograma funcionam. **Não repetir "o banco DEV está vazio" como fato: era verdade em 2026-08-31 de manhã e deixou de ser.** | `scripts/check-remote-schema.mjs`, `apps/mobile/src/app/index.tsx`, runbook, `package.json` | **RESOLVIDO (medido, 2026-08-31)** |

| D-88 | SPEC-016 — Beta Experience: identidade visual e design system | **APPROVED v0.1 (agente, §0.2/§0.3, a pedido explícito do dono).** Milestone de transformar o produto funcional em produto apresentável antes do beta. **Nada muda regra, schema, autorização ou domínio capilar** — é apresentação, e apresentação é reversível arquivo a arquivo; por isso não é human gate. **Contexto: nenhum** — SPEC transversal de apresentação, que não cria conceito de domínio e não entra no DOMAIN-MAP. **Identidade decidida:** base neutra **quente** (osso/areia) em vez de cinza frio — a diferença entre "clínico" e "cuidado" · tinta grafite quente em vez de preto puro · **um** acento saturado, ameixa profunda, adulto e feminino sem o clichê de rosa que o brief exclui · cores **semânticas** por tipo de cuidado (hidratação/nutrição/reconstrução), que informam em vez de decorar · cantos suaves, elevação quase imperceptível. **Escala tipográfica e de espaçamento de 4** substituem valores soltos: literal de cor ou padding em tela de produto passa a ser bug (FR2). **Sem dependência nova** (NG3): fonte custom, ícones, dark mode e motion ficam como OQ com gatilho — a stack do sistema bem usada chega longe, e cada uma delas seria §4 + carregamento + FOUT. **Onboarding vira fluxo em etapas** com as **mesmas** perguntas, opções, exclusividade e validação da SPEC-002; a equivalência é testada (AC2). **Nome/marca do produto continua decisão do dono** (OQ5) — a identidade foi construída sem depender de um nome. | SPEC-016 → Approved; `apps/mobile/src/design/`; SignIn e Onboarding redesenhados | **DECIDED (agente, §0.2)** |
| D-89 | SPEC-016 fatia 2 — a Hoje ganha hierarquia (cartão de foco, week strip) | **DECIDED (agente, §0.2/§0.3).** Fatia de apresentação dentro de SPEC aprovada; **nenhuma regra, schema, autorização, RPC ou contrato de port foi tocado** (AC6 verificado). **O problema não era feio, era plano:** quatro seções de peso idêntico, cada linha com quatro botões de peso idêntico — "o que eu faço agora?" só podia ser respondido lendo tudo. **Decidido:** um **cartão de foco** responde a pergunta e é o único lugar da tela com botão primário preenchido, o que torna "uma ação primária por tela" invariante estrutural (`emphasis`) em vez de intenção. Prioridade do foco: cuidado recém-registrado enquanto tiver check-in a responder ou desfazer aberto → atrasado mais antigo (D-28: o plano nunca se move sozinho) → o de hoje. **Fecha um buraco real:** concluir um cuidado atrasado o torna `done` num dia passado e `buildTodayView` o arquiva em histórico, então o "Como ficou?" pulava para o fim da tela. **Week strip** informativo, não clicável (tocar num dia teria de significar navegar ou filtrar, e nenhum dos dois existe); `buildWeek` fica em `apps/mobile`, **não** em `packages/core` — não decide nada de domínio, e "semana começa no domingo" e "como o dia é lido em voz alta" são escolhas de locale, que são da UI. **Rejeitado por necessidade:** colapsar "Próximos"/"Histórico" (tiraria "Como fazer" de cuidado acionável, SPEC-007 AC5), tab bar (três destinos, D-47), motion/ícones/fonte custom (§4, OQ1/OQ2/OQ4), copy "emocional" com orientação capilar (gate D-26/D-70). **Auditoria `improve`:** `inkFaint` medido em **3.09:1**, reprovado em WCAG AA, corrigido para **4.97:1** — conserta também os usos da fatia 1 sem tocar naqueles arquivos; escrita em cartão fora do que estava em voo ficava acesa e silenciosamente não fazia nada, agora se declara desabilitada; `Button variant="danger"` (sem consumidor, texto invisível quando pressionado), tonalidades de cuidado do `Tag` e rótulo "Planejado" removidos por AC3/D-47. | `apps/mobile/src/features/care/*`, `apps/mobile/src/design/*`, SPEC-016 §20/§24 | **DECIDED (agente, §0.2)** |

| D-90 | Edge Functions nunca foram deployadas no projeto DEV — e "verde" não é validação | **DIAGNOSTICADO (agente, medido 2026-08-31).** Com o banco DEV já provisionado (D-87 resolvido) e a jornada real rodando até o preview, **"Começar meu cronograma" falhava** com *"Não foi possível criar seu cronograma. Tente novamente."* **Causa raiz medida, não inferida:** `POST /functions/v1/generate-plan` responde **`404 {"code":"NOT_FOUND"}`** — exatamente igual a uma função que não existe. Nenhuma das 3 funções (`generate-plan`, `health`, `billing-webhook`) está deployada em `ayecidupmxmirwfzwtea`. O bootstrap SQL aplicou **migrations**; Edge Function é um deploy separado. Como `create_plan_tx` é **service_role only** (SPEC-004 §12/G2/P10), sem a função **não existe caminho** para criar um plano — e **não há correção de código que contorne isso sem enfraquecer autorização**, o que não se faz. **A repetição do padrão de D-87 uma camada acima:** `pnpm verify`, pgTAP e `deno test` conferem o repositório; nenhum deles pergunta se o ambiente para onde o app aponta tem a coisa que o app chama. **Entregue autonomamente:** (a) `scripts/check-remote-functions.mjs` + `pnpm check:remote` — prova sem credencial quais funções faltam (404 NOT_FOUND é a resposta para qualquer chamador); (b) **a falha passa a ser diagnosticável** — `functions.invoke` colapsa todo não-2xx numa mensagem constante e inútil, então o adapter passa a carregar status e corpo, e `PlanScreen`/`TodayScreen` os mostram **só sob `__DEV__`** (mesma lição de D-87, que não tinha sido aplicada nesta camada); (c) helper `reasonOf` extraído para um único lugar; (d) runbook `DEV-EDGE-FUNCTIONS-DEPLOY.md`. **Segundo defeito, achado só rodando o app (e é o ponto da regra nova):** a função **não tratava CORS**. `functions.invoke` manda `Authorization` + `Content-Type: application/json`, o que torna a requisição não-simples, então o navegador manda um `OPTIONS` antes — e o handler respondia `405` sem cabeçalho CORS nenhum. Ou seja: **mesmo depois do deploy, a jornada no preview web continuaria falhando**, com a mesma mensagem opaca. Corrigido em `cors.ts` + 4 testes Deno. `*` não enfraquece nada: o JWT continua exigido e validado, e **não há `Allow-Credentials`** — sem ele o navegador não anexa credencial ambiente, e o token vive no storage da origem do app. Um teste trava justamente a combinação perigosa (`*` + credentials) para ninguém "consertar" CORS assim depois. **TRUE HUMAN GATE para o deploy:** exige Supabase CLI ou `SUPABASE_ACCESS_TOKEN` — medido, ambos ausentes nesta máquina. **Regra de governança derivada, decidida pelo dono e gravada em CLAUDE.md §0.1:** CI/testes/typecheck **não bastam** para DONE quando existe DEV real; a jornada afetada tem de ser executada e observada de ponta a ponta; e **estado medido vence documentação histórica**. | `scripts/check-remote-functions.mjs`, `apps/mobile/src/shared/failure-detail.ts`, `hair-plan-adapter.ts`, `PlanScreen.tsx`, `TodayScreen.tsx`, `app/index.tsx`, runbook novo, `package.json`, `CLAUDE.md` §0.1 | **DECIDED (agente, §0.2) + regra humana** |

| D-91 | Strategy A (D-49) nunca sobreviveu a um deploy: o grafo do `generate-plan` sai de `supabase/` | **DIAGNOSTICADO E CORRIGIDO (agente, medido 2026-08-31).** O primeiro deploy real falhou: `billing-webhook` subiu, `generate-plan` morreu em `Error: failed to create the graph — Module not found ".../packages/core/src/index.ts"`. **Causa:** SPEC-000 §20 / D-49 escolheu consumir `packages/core` direto das fontes do workspace, mapeado em `supabase/functions/deno.json` como `@app/core → ../../packages/core/src/index.ts`. Isso funciona em tudo que já tinha sido exercitado — `deno check`, `deno test`, `deno lint`, o core smoke — porque **todos rodam sobre o repositório**. O bundler que deploya enxerga só `supabase/functions`, e `../../packages/core` está fora. `billing-webhook` passou porque importa apenas `./webhook.ts` e specifiers npm. **A mesma família de D-87/D-90 pela terceira vez no mesmo dia: verde sobre o repositório não diz nada sobre o ambiente.** **Decisão:** **não** reabrir D-49. A estratégia continua correta para desenvolvimento; o que faltava era materializar o grafo na hora do deploy. `scripts/stage-edge-bundle.mjs` copia `packages/core/src` para `supabase/functions/_core/` e gera um import map que aponta para a cópia, **carregando todos os outros mapeamentos do `deno.json` byte a byte** — os dois artefatos são derivados e gitignored, exatamente como o bootstrap SQL (#53), porque uma cópia versionada de core seria uma segunda fonte de verdade envelhecendo em silêncio. `deno.json` segue sendo o único mapeamento escrito à mão, e `check-deno-import-map.mjs` continua guardando ele. **Guardrail:** `core-deno.yml` passa a type-checkar **o grafo deployável** (bundle staged + import map de deploy) em toda PR — a checagem que faltava; e o workflow de deploy exige `OPTIONS → 204` depois de subir, então um deploy sem a correção de CORS falha. | `scripts/stage-edge-bundle.mjs`, `.github/workflows/{deploy-dev-functions,core-deno}.yml`, `.gitignore`, `package.json` | **DECIDED (agente, §0.2)** |

| D-92 | MASTER PRODUCT SCOPE — o escopo aprovado do produto, e quem decide a ordem | **DECISÃO HUMANA (dono, 2026-08-31).** Todas as capabilities listadas em `docs/product/MASTER-PRODUCT-BACKLOG.md` são **COMMITTED**: fazem parte oficialmente do produto e **serão construídas**. Não são brainstorm, ideias opcionais, sugestões nem itens que dependem de o dono pedir de novo. Uma capability só sai por: (a) evidência técnica de que deve ser **substituída** por solução melhor; (b) decisão comercial/produto **explícita** do dono; (c) impedimento legal, de segurança ou de domain review. **31 capabilities Free · 23 Premium · 1 transversal (Community).** **A pergunta não é "isso vai ser feito?" — é "qual é o melhor momento e a melhor forma?".** **Sequenciamento é do agente:** quando cada capability entra, dependências, divisão em SPECs e fatias, arquitetura, quais dados precisam existir antes, quando estabilizar em vez de empilhar camada, quando refatorar, quando adiar por dependência técnica real. Estados obrigatórios: `COMMITTED`/`IN PROGRESS`/`DONE`/`BLOCKED`/`DEFERRED BY DEPENDENCY` — **"future idea" não é estado**, e `DEFERRED BY DEPENDENCY` significa "vai ser construída, mas não agora", nunca "talvez". **Duas restrições de ordem que NÃO são do agente:** 🔒 o **Assistente IA é obrigatoriamente a última grande capability** — é COMMITTED, será construída, e até lá é **proibida infraestrutura antecipada de IA** (embeddings, pgvector, RAG, agentes, chatbot, API de LLM, tabelas de chat, prompts de produção, abstrações de LLM): *a IA será construída sobre o sistema, não o sistema em torno da IA*; 🔒 **Community fica DEFERRED BY DEPENDENCY** por bastante tempo (escala, moderação, segurança, privacidade, massa crítica) e **não sai do roadmap**, sem infraestrutura social prematura. **Um único plano pago — PREMIUM**, mensal e anual com **exatamente as mesmas funcionalidades**; referência comercial **R$ 19,90/mês · R$ 149,90/ano · trial de 7 dias**, com preço e período **vindos da loja em runtime, nunca hard-coded** (D-83 inalterada). **Hair Intelligence começa determinística**, sem IA: dados → histórico → métricas → agregações → comparação → padrões → insights, **sem inventar causalidade nem dados**. **Recomendações preferem sempre o que a usuária já possui**, e monetização futura nunca degrada confiança — nada de inventar produto, composição, indicação, preço, link ou benefício. **Wash Day é estrutural**, não tela de anotação: o modelo de dados decidido no Free é o que viabiliza (ou inviabiliza) Smart Shelf, padrões e Wash Day avançado depois. **Limites da autonomia (TRUE HUMAN GATES) reafirmados:** existência de Free + Premium · novos tiers · preço aprovado · estratégia de monetização material · lançamento em produção · contratos/custos · decisões legais · domínio clínico/profissional · mudança material de marca. | `docs/product/MASTER-PRODUCT-BACKLOG.md` (novo), CLAUDE.md §0.4, `docs/product/MVP-ROADMAP.md` | **HUMAN DECISION (dono)** |

| D-93 | `improve --full` — checkpoint de beta (SPEC-016 fatia 5) | **AUDITORIA EXECUTADA (agente, 2026-08-31).** Varredura de projeto inteiro antes do beta. **Zero BLOCKER.** **O que está sólido, e vale dizer:** RLS tem uma **guarda genérica sobre toda tabela pública** (`001_rls_enabled_on_all_public_tables`) mais uma **fixture negativa que prova que a guarda detecta** (`004`) — uma tabela nova não passa despercebida; allowlists de grants e de `SECURITY DEFINER` idem; as 10 tabelas das migrations conferem, `consents` corretamente ausente e documentada como pendência de release (D-32); **nenhum TODO/FIXME/HACK no repositório**, nenhum `console.*` vazando fora dos dois erros de servidor documentados e do smoke de CI, nenhum teste `.only`/`.skip`. **IMPORTANT — documentação contradizendo a implementação, corrigido:** CLAUDE.md §0 afirmava que faltavam as fatias 3 e 4 da SPEC-016 (ambas mergeadas, #60/#62); SPEC-016 §20 não marcava a fatia 4; e `MVP-ROADMAP.md` ainda dizia *"Draft v0.1 — aguardando aprovação"* com as fases 0–9 entregues, sem qualquer ponteiro para o MASTER PRODUCT BACKLOG. **Documentação de estado desatualizada não é cosmética** — repeti uma entrada histórica como fato atual hoje mesmo (D-87) e custou tempo do dono; por isso a reconciliação é achado IMPORTANT, não OPTIONAL. **OPTIONAL, reportado e não alterado (cruza fronteira de SPEC-000):** quatro exports mortos em `packages/core` — `generateSchedule` (alias de `generateScheduleV1` sem consumidor), `noopAnalytics` (implementação de porta sem consumidor; analytics adiado por D-31), `isAfter` e `isBefore`. Sem cenário de falha, custo zero hoje; remoção merece PR própria em vez de edição de passagem num checkpoint. **AC1 verificado por varredura, não presumido:** zero literal de cor e zero número solto de espaçamento/tamanho fora de `apps/mobile/src/design/` em tela de produto. **Gates de beta reafirmados, todos humanos e nenhum de engenharia:** Auth real (D-86) · sign-off de domínio capilar (D-26/OQ-REL) · base legal LGPD + `consents` (D-32) · purchase flow/IAP (D-79). | CLAUDE.md §0, SPEC-016 §20, `docs/product/MVP-ROADMAP.md` | **DECIDED (agente, §0.1)** |

| D-94 | MASTER PRODUCT BLUEPRINT — a intenção funcional de cada capability | **DECISÃO HUMANA (dono, 2026-08-31).** Complemento canônico do MASTER PRODUCT SCOPE (D-92), que **não altera nada** do que já foi aprovado: as 55 capabilities seguem COMMITTED. O backlog responde *"o que falta construir"*; o Blueprint (`docs/product/MASTER-PRODUCT-BLUEPRINT.md`) responde *"o que cada capability significa funcionalmente para a usuária"* — objetivo, problema, tier, entry points, fluxo, inputs/outputs, dados conceitualmente necessários, regras, relações, estado sem dados suficientes, privacidade, **o que NÃO deve fazer**, critério de sucesso e dependências. **Motivo:** nome não sobrevive sozinho — daqui a seis meses "Smart Shelf" e "Wash Day" são palavras que qualquer um reinterpreta de um jeito plausível e errado; o documento foi escrito para quem não participou da conversa. **Não é SPEC:** define O QUE e POR QUÊ, nunca tabela, RPC, endpoint, componente, biblioteca ou schema. **Decisão editorial (agente):** tratamento funcional completo para tudo que **ainda não existe**, que é onde a intenção corre risco; as capabilities **DONE** apontam para a SPEC, que já preserva a intenção com mais autoridade do que um resumo — recontar criaria uma segunda fonte de verdade envelhecendo em silêncio, o erro que este projeto já pagou em D-87 e D-93. **Princípios fixados como inequívocos:** *FREE executa e registra · PREMIUM interpreta, compara, aprende e adapta*; **registrar nunca é premium** (cobrar pelo registro seca a base de que o Premium depende); um único tier pago, mensal e anual idênticos, R$ 19,90/mês · R$ 149,90/ano · trial de 7 dias como referência, preço da loja em runtime (D-83); e três proibições de honestidade — **não inventar causalidade, não inventar dados, não diagnosticar** — com a regra de que, sem dados suficientes, o produto **diz que ainda está aprendendo** em vez de preencher a interface. **Wash Day é reafirmado como estrutural:** o modelo de dados decidido no Free é o que viabiliza ou inviabiliza Smart Shelf, padrões, relatórios e adaptativo; texto livre ali destruiria metade do Premium. **Assistente IA** continua COMMITTED e **obrigatoriamente último**, como interface sobre o sistema — *o sistema produz os fatos, a IA ajuda a consultá-los* — com a proibição de infraestrutura antecipada de LLM inalterada. **A autonomia do agente não muda:** o dono define o que resolver; o agente decide quando, em que ordem, com qual arquitetura, SPECs, fatias, modelo de dados e UX — e, se houver caminho melhor para o mesmo objetivo funcional, usa-o; só é decisão material se alterar o objetivo ou a proposta de valor. | `docs/product/MASTER-PRODUCT-BLUEPRINT.md` (novo), `MASTER-PRODUCT-BACKLOG.md`, CLAUDE.md §0.4 e §9 | **HUMAN DECISION (dono)** |

| D-95 | `profiles` nasce na SPEC-018 — e com uma coluna só | **DECIDIDO (agente, §0.2, 2026-09-01).** D-63 removeu `profiles` da SPEC-002 dizendo que ela nasceria "numa SPEC futura quando houver requisito concreto". O requisito chegou: a primeira experiência da Huna pergunta **como a usuária quer ser chamada** (SPEC-018 FR7). A tabela nasce com **`display_name` e nada mais**. **O que continua não existindo, de propósito:** `timezone` — o fuso IANA viaja na chamada e não é retido (ADR-008); `locale` — sem consumidor, o app é pt-BR; `onboarding_status` — **D-63 o rejeitou explicitamente** ao derivar "onboarding concluído" da existência de um `hair_profiles`, e essa derivação continua sendo a verdade. Criar as três "já que a tabela vai existir" é exatamente o erro que D-47/D-48 proíbem. **A decisão de modelo que importa:** `display_name` é nullable e a **existência da linha** é o que registra que a pergunta foi feita — sem essa distinção, o app perguntaria o nome a cada abertura exatamente a quem escolheu não responder. **Posse por RLS, sem RPC:** não há invariante de servidor a proteger, é a declaração dela sobre ela mesma — como `plan_preferences` (D-81), ao contrário de planos e execuções. Sem `DELETE`: a linha morre por cascade a partir de `auth.users`, e "apagar meu nome" é um `UPDATE` para nulo. **Privacidade:** é o **primeiro dado pessoal declarado pela usuária** no produto — `DATA-MODEL` §4.1 passa a listá-lo, §4.2 deixa de dizer que não coletamos nome. Não cria consentimento novo nem antecipa a base legal de D-32: é dado que ela fornece para a função que ela pediu. Pequeno e reversível (aditivo, sem backfill) ⇒ §0.2. | SPEC-018 §8–§12; `DATA-MODEL.md` §3.1/§4.1/§4.2 e diagrama ER; migration `20260904000000_profiles.sql` | **DECIDED (agente, §0.2)** |

| D-96 | `hair_events` — registrar sem interpretar | **DECIDIDO (agente, §0.2/§0.4, 2026-09-01).** O MASTER PRODUCT BLUEPRINT §6 nomeia o maior risco do produto: ela descolore o cabelo numa sexta e o cronograma de segunda continua o mesmo, montado para um cabelo que não existe mais — e o app nem sabe. A tabela `hair_events` é o registro de que ela contou. **A decisão que define a capability é o que ela NÃO faz:** nenhuma regra lê `event_type` para decidir cuidado, nenhum texto diz o que fazer depois de uma química, e nada qualifica o cabelo dela. É essa contenção — não uma revisão de domínio — que mantém o F23 **fora** do gate D-26/D-70, e ela se perde na primeira palavra a mais, então tem barreira de teste com amostras que precisam casar. **Escrita só por RPC `SECURITY DEFINER`**, por dois invariantes de servidor: o dia civil ("um evento não pode ser no futuro", validado por `care_local_today`, que já limita o fuso recebido, T22) e a idempotência por `(user_id, client_event_id)`. O `user_id` **nunca** é parâmetro. O cliente tem **só SELECT** da própria linha e **nenhum DELETE**: anular marca `voided_at` e a linha fica, como `care_executions` (D-69/D-12). **Sem texto livre** (seria a primeira PII de forma livre do produto, sem consumidor) e **sem FK para plano**, porque amarrar o evento a um plano o faria sumir na próxima substituição — o oposto do objetivo. **Oferece, não impõe:** reavaliar é escolha com duas saídas igualmente claras (mesma regra de D-28). | SPEC-020; `DATA-MODEL.md` §3.2b/§4.1; migration `20260905000000_hair_events.sql`; pgTAP `100_spec020_hair_events.sql` | **DECIDED (agente, §0.2/§0.4)** |

| D-97 | Autonomia: decidir é do agente; parar é exceção | **DECISÃO HUMANA (dono, 2026-09-01).** O dono determinou explicitamente que **não quer ser transformado em aprovador** de decisões que o agente tem informação e competência para tomar. Uma decisão **não** vira human gate quando é técnica ou de produto **reversível**, não altera materialmente o modelo comercial, não gera custo ou contrato, não cria obrigação legal, não envolve produção irreversível, não ultrapassa gate profissional/de domínio, tem alternativas analisáveis objetivamente, e existe uma opção **claramente superior** por arquitetura, UX, invariantes, evidência ou simplicidade. Nesses casos o ciclo é: investigar → modelar → avaliar impacto → escolher → **registrar** → implementar → testar → **validar no DEV real** → corrigir BLOCKER/IMPORTANT → continuar, e **reportar depois com a justificativa**. **Product judgment não é human gate:** organização de tela, hierarquia, navegação, divulgação progressiva, microcopy não material, estados, interação, estrutura de capability, momento de introduzir função, schema, RPC, contrato, refactor, estratégia de testes, sequência de implementação e resolução de inconsistência são do agente quando reversíveis. A pergunta é *"consigo decidir isto corretamente com a visão do produto, o Blueprint, a evidência e o estado real?"* — se sim, decide. **Proibido criar OQ apenas para transferir responsabilidade.** **TRUE HUMAN GATES permanecem** e são a única razão para parar: gasto financeiro real · contratação · assinatura de serviço · credencial que só o dono possui · decisão jurídica ou material de privacidade · alteração material de preço ou modelo comercial · novo tier pago · mudança fundamental da proposta do produto · mudança material de marca · publicação irreversível em produção · App Store/Play Store quando exigir o titular · domain review profissional · **escolha subjetiva de produto sem alternativa objetivamente superior cuja diferença altere materialmente a experiência**. Mesmo nesses, o agente traz opções + impacto + recomendação. | CLAUDE.md §0.3 (amendada); §0.1 lida por este filtro | **HUMAN DECISION (dono)** |
| D-98 | OQ2 da SPEC-022 e OQ1 da SPEC-017 | **DECISÃO HUMANA (dono, 2026-09-01), com as recomendações do agente.** **SPEC-022/OQ2 — opção 1:** ao retomar, deslocar o restante do plano preservando os intervalos originalmente calculados pelo engine; **o fim do ciclo é o limite natural** — quando o deslocamento deixar de caber nas quatro semanas, a volta oferece um ciclo novo em vez de desenhar uma quinta semana. Preserva histórico, evita avalanche, mantém previsibilidade e explica **antes** de confirmar. A opção 2 (deslocar só o que caiu na pausa) foi descartada por comprimir justamente os intervalos que o engine escolheu — a avalanche reetiquetada. **SPEC-017/OQ1 — opção A:** a evidência vem do snapshot que **originou** o plano ativo (`hair_plans.hair_profile_id`), nunca do perfil corrente; **não persistir dado derivável apenas para evitar leitura**. A explicação tem de corresponder ao plano que ela realmente possui, inclusive no caso reavaliação → cancelamento → plano anterior continua ativo. | SPEC-022 §23 OQ2/OQ3; SPEC-017 §23 OQ1, §8, §9 | **HUMAN DECISION (dono)** |
| D-99 | Catálogo de produtos reais e scanner/EAN entram no roadmap como capabilities próprias | **DECISÃO DO AGENTE (§0.4/D-97), a partir de instrução explícita do dono (2026-09-01).** O catálogo existia no Blueprint apenas como *"catálogo controlado"*, uma **dependência** de `P18`/`P19` — e o scanner não existia em lugar nenhum. Uma capability COMMITTED que só aparece como dependência de outra é uma capability que some no primeiro replanejamento, o que §0.4 proíbe. Agora são `F32` (marcas e produtos reais: nome, linha, categoria, imagem de **fonte autorizada**, variante/tamanho, busca, "adicionar à minha prateleira", com o cadastro manual como **fallback**) e `F33` (identificação por EAN). Estado `DEFERRED BY DEPENDENCY`, **não** `COMMITTED` sem qualificação, porque o que trava não é engenharia: curadoria de dados reais, direito de uso de imagem e possível custo/contrato são TRUE HUMAN GATE, e o scanner ainda soma a primeira dependência nativa de câmera. **Nenhum dos dois substitui a prateleira manual** — chegam por cima da mesma linha em `products`, e um catálogo que não encontra o vidro dela não pode virar um beco. | MASTER-PRODUCT-BACKLOG `F32`/`F33`; MASTER-PRODUCT-BLUEPRINT §10; CLAUDE.md §0 | Registrada |
| D-100 | O que o Catálogo Huna de produtos reais é, e o que ele nunca pode virar | **DECISÃO HUMANA (dono, 2026-09-02).** O `F32` é COMMITTED e a experiência-alvo é: pesquisar **marcas** e **produtos** reais; ver marca, produto, linha, categoria, variante/tamanho quando aplicável, **imagem real de fonte autorizada** e EAN quando disponível; **adicionar o produto real à Minha Prateleira**; **selecioná-lo no Wash Day**; e **preservá-lo no histórico mesmo depois de arquivado**. **O produto manual continua obrigatório como fallback** (*"Não encontrou seu produto?"*): o catálogo **facilita o cadastro da prateleira, não a substitui**. Prioridade conceitual: **Catálogo → Prateleira → Wash Day → Smart Shelf → Hair Intelligence → Recomendações**. **Nada é inventado** — produto, composição, benefício, preço ou URL. Fotos, logos e embalagens exigem **direito de uso adequado**, e **scraping improvisado está fora**: quando chegar a hora, a fonte de dados precisa ser sustentável e legalmente utilizável. **Brasil primeiro** na curadoria, internacional depois. A interface é **busca**, não uma parede de centenas de logos — marcas populares fáceis de achar sem sacrificar a simplicidade. **Scanner/EAN (`F33`) é etapa posterior**, nunca simultânea. | MASTER-PRODUCT-BACKLOG `F32`/`F33`; MASTER-PRODUCT-BLUEPRINT §10; [[D-99]] | **HUMAN DECISION (dono)** |


## C. DEFERRED / OPEN (não decidir agora)

### C1. Decisões de implementação da SPEC-001 (não reabrem a SPEC)
| ID | Decisão | Classe | Recomendação provisória | Quando |
|---|---|---|---|---|
| D-56 | Integração Apple/Google: SDK nativo vs OAuth por browser | **DECIDED (implementação SPEC-001, 2026-08-27)** | **OAuth por browser com PKCE** (`signInWithOAuth` + `expo-web-browser` `openAuthSessionAsync` + `exchangeCodeForSession`) para **ambos** os provedores: um único mecanismo, sem SDK nativo, sem dev build, sem rota de deep link (o browser devolve a URL de callback à função). Redirect `haircare://auth/callback` (+ `exp://` só em dev) allowlistado. Reavaliar SDK nativo só se a UX do browser se provar um problema medido | `apps/mobile/src/infrastructure/supabase/auth-adapter.ts` |
| D-57 | Provedor SMTP para OTP | IMPORTANT BEFORE RELEVANT IMPLEMENTATION (staging/prod) | inbox de teste local até lá | antes de OTP em staging |
| D-58 | Credenciais Apple Developer / Google Cloud, keystores, dev builds | IMPORTANT BEFORE RELEVANT IMPLEMENTATION | — | antes dos testes reais dos provedores |
| D-59 | Implementação concreta do storage seguro | **DECIDED (implementação SPEC-001, 2026-08-27)** | **`expo-secure-store`** como storage adapter do supabase-js; valores divididos em chunks de 1800 bytes (limite ~2 KB por chave; chunking não é criptografia — o keychain/keystore cifra); indisponível → sessão só em memória (nunca storage inseguro); reinstalação detectada por marcador em `expo-file-system` (`Paths.document`) → sessão residual descartada | `secure-session-storage.ts`, `fresh-install.ts` |
| D-60 | Política de exclusão: imediata vs grace period (+ sessão pós-pedido) | HUMAN DECISION — IMPORTANT BEFORE RELEVANT IMPLEMENTATION | não fixar; tabela registra só `requested_at` | antes do job de purga / release |
| D-61 | Caminho externo/web para solicitar exclusão (Google Play) | RELEASE REQUIREMENT | SPEC-013 | antes do release Android |

| ID | Decisão | Recomendação provisória | Quando decidir | Custo tardio | Status |
|---|---|---|---|---|---|
| ~~D-12~~ | movido para B5 (**DECIDED** 2026-08-27: sim, janela de 15 min) | — | — | — | DECIDED |
| D-30 | Nome do produto | placeholder "Hairo" | Antes da fase 10 | L/M | OPEN |
| D-31 | Provider analytics / crash | PostHog + Sentry | Fase 10 | L | DEFERRED |
| D-32 | Base legal LGPD para analytics | consentimento | Antes da fase 10 (jurídico) | L | OPEN |
| D-33 | Lib de datas (`date-fns-tz` vs `Temporal`) | date-fns-tz | **SPEC-000** (proposta em §Dependencies) | L | OPEN → proposta em SPEC-000 |
| D-34 | Janela de geração de `scheduled_cares` | 8 semanas | SPEC-004 | L | DEFERRED |
| ~~D-35~~ | movido para B5 (**DECIDED** 2026-08-27: 0 ou 1 execução efetiva) | — | — | — | DECIDED |
| D-36 | Estado global (Zustand vs Context) | Zustand só se necessário | SPEC-001+ | L | DEFERRED |
| D-37 | E2E: Maestro vs Detox | Maestro | Fase 10 | L | DEFERRED |
| D-38 | Captcha em signup/OTP | sim antes do lançamento | SPEC-001/013 | L | DEFERRED |
| D-39 | Retenção de `audit_log` e analytics | 12 meses | Antes da fase 10 | L | DEFERRED |
| ~~D-40~~ | movido para B2 (decidido: Estratégia A) | — | — | — | DECIDED |

## D. Histórico
| Data | Mudança |
|---|---|
| 2026-08-26 | v0.1 criado na Foundation Review. |
| 2026-08-26 | v0.2: aprovação humana B1 (D-01…D-11, D-13…D-15); ADR-002 aprovada; D-21…D-28 decididas/adiadas conforme registro humano. Fase autorizada: SPEC-000. |
| 2026-08-26 | v0.3: revisão da SPEC-000 (D-41…D-49); implementação concluída (D-33, D-40 resolvidos); close-out com CI verde; D-50 AC12 deferred. |
| 2026-08-26 | v0.4: SPEC-001 aprovada (D-51…D-55); decisões de implementação rastreadas (D-56…D-61). Implementação de autenticação **não** iniciada. |
| 2026-08-27 | v0.5: SPEC-001 implementada e mergeada em `main` (PR #3) via LEVEL 2 auto-merge; required CI verde (`ci`, `core-deno`, `supabase-test`; pgTAP 13/13). D-56 e D-59 decididos na implementação. Proteção da `main` no GitHub habilitada (required checks + PR + strict + enforce_admins; force-push/deleção bloqueados). |
| 2026-08-27 | v0.6: correção de governança CI (PR #5, merged) — `core-deno`/`supabase-test` rodam em todo PR (required checks satisfazíveis). Decisões de produto da SPEC-002 (D-62/D-63/D-64); D-64 amenda D-11 (snapshots por `id`, sem numeração sequencial). SPEC-002 → Draft Ready for Approval (HUMAN GATE). |
| 2026-08-27 | v0.7: **SPEC-002 APPROVED** (D-65) com clarificações (`chemical_treatments` `[]`=nenhuma; `no_major_concern` exclusivo; analytics DEFER). Implementação autorizada (LEVEL 2). |
| 2026-08-27 | v0.8: **SPEC-002 implementada e mergeada** em `main` (PR #6) via LEVEL 2; required CI verde (pgTAP 020 18/18). `hair_profiles` imutável por `id` (D-64), sem `profiles` (D-63), 8 inputs (D-62). Pendente doc-sync de DATA-MODEL/DOMAIN-MAP. |
| 2026-08-27 | v0.9: doc-sync DATA-MODEL/DOMAIN-MAP/CLAUDE.md feito (PR #8). **SPEC-003 FOLDED INTO SPEC-004** (D-66; ADR-007 Amendment A2) — não implementada, fronteira Diagnostic preservada. |
| 2026-08-27 | v0.10: SPEC-004 modelo técnico CLOSED; **regras de domínio V1 CANDIDATE (D-67)**; ADR-007 A1 ganha status `candidate` (implementável em dev/internal beta; PUBLIC RELEASE exige `validated`). |
| 2026-08-27 | v0.11: **SPEC-004 APPROVED (D-68)** por aprovação humana; implementação autorizada (LEVEL 2). PUBLIC RELEASE continua bloqueado até o domain sign-off das regras V1 (`candidate → validated`). |
| 2026-08-27 | v0.12: **SPEC-004 implementada e mergeada** em `main` (PR #11) via LEVEL 2; required CI verde (pgTAP 030 29/29). Engines `assess`/`generateSchedule` v1 puros com golden; `hair_plans`/`scheduled_cares` + RPC `create_plan_tx` (advisory lock por usuária, idempotência por `client_request_id`, FK composta de ownership); Edge `generate-plan`; preview/confirmação no app. **Nenhuma decisão nova**: implementação dentro de D-66/D-67/D-68. **Único bloqueio remanescente para PUBLIC RELEASE: OQ-REL — sign-off de domínio das regras V1.** |
| 2026-08-27 | v0.13: **SPEC-005 APPROVED (D-69)** por aprovação humana, com **D-12** (desfazer execução: sim, janela de 15 min, registro anulado preservado) e **D-35** (0 ou 1 execução efetiva por cuidado, garantido no banco) **decididas** e movidas de C para B5. `status='completed'` não persistido. Implementação autorizada (LEVEL 2). MVP-ROADMAP sincronizado: o checkpoint de especialista "antes de F5" foi re-escopado por D-67/D-68 — **o gate de PUBLIC RELEASE permanece inalterado**. |
| 2026-08-27 | v0.14: **SPEC-005 implementada e mergeada** em `main` (PR #14) via LEVEL 2; required CI verde (pgTAP 040 41/41). Loop diário completo: Hoje/atrasado/próximos/histórico + concluir, pular, reagendar e desfazer (15 min). `care_executions` com índice único parcial garantindo 0 ou 1 execução efetiva (D-35); `voided_at` preserva o registro anulado (D-12); conclusão e atraso **derivados**, sem coluna. **Nenhuma decisão nova**: implementação dentro de D-28/D-69. **Bloqueio remanescente para PUBLIC RELEASE: OQ-REL — sign-off de domínio das regras V1 (D-26/D-67), inalterado por esta fatia.** |
| 2026-08-28 | v0.15: **CLAUDE.md §0.2 — Master Autonomous Engineering Mode** autorizado por decisão humana (PR #18): autonomia técnica plena dentro de SPEC aprovada; human/external gates estreitados para o material/irreversível; §6 reconciliado com o LEVEL 2 (o GitHub executa o merge após as required checks). Skill **`improve`** instalada como auditoria obrigatória antes de DONE (PR #17). Novo bloco **B6**: **D-70** (conteúdo capilar V1 nasce `candidate`, aplicando o precedente D-67, e entra em OQ-REL), **D-71** (conteúdo no bundle, sem `care_types`/`content_articles`, com gatilho nomeado para reabrir) e **D-72** (**SPEC-007 APPROVED**) — decididas pelo agente sob §0.2, pequenas e reversíveis, sujeitas a ratificação humana. **O gate de PUBLIC RELEASE permanece inalterado.** |
| 2026-08-28 | v0.16: **SPEC-007 implementada e mergeada** em `main` (PR #19) via LEVEL 2; required CI verde; pgTAP **inalterada em 115 asserções** (nenhuma SQL tocada). Guias "como fazer" por care type na tela Hoje, no bundle (D-71), `candidate` (D-70). **Fase 5 do roadmap fechada.** **D-70/D-71/D-72 RATIFICADAS por decisão humana**, D-70 com correção: o gate segue o **conteúdo**, não o autor — texto com orientação capilar substantiva exige sign-off do revisor de domínio mesmo vindo do owner (SPEC-007 BR2b); **OQ-REL não foi enfraquecida**. Auditoria `improve`: 1 passe, 3 IMPORTANT corrigidos, 0 BLOCKER — o principal foi um teste AC4 cujas âncoras `\b` tornavam duas verificações incapazes de casar (proteção inexistente com CI verde). |
| 2026-08-28 | v0.17: **SPEC-006 implementada e mergeada** em `main` via LEVEL 2 (**D-73**, agente §0.2 — aguarda ratificação). Check-in de uma pergunta (1..5) ancorado na execução efetiva; `checkins` com RLS ON+FORCE, grant só de SELECT, escrita só por `submit_checkin` (DEFINER allowlistada, `search_path` fixo, idempotente, recusa execução anulada/alheia). pgTAP passa a **141 asserções** (115 + 26). Necessity review: 4 dimensões e `note` continuam DEFER. **Nenhuma regra capilar nova — D-26 não acionada; gate de PUBLIC RELEASE inalterado.** Auditoria `improve`: 1 BLOCKER (a tela não passava `checkIns` para `buildTodayView` — compilava e o core passava, mas nenhum check-in apareceria) e 2 IMPORTANT corrigidos; 1 IMPORTANT **reportado sem corrigir**: `tests.unapproved_security_definer_functions()` não verifica o pin de `search_path`, e endurecer o helper compartilhado é fatia própria. |
| 2026-08-28 | v0.18: **D-73 RATIFICADA** por decisão humana (check-in de uma pergunta 1..5, sem texto livre, sem edição; as demais dimensões entram anuláveis na SPEC-009). **Guardrail de `search_path` endurecido** (fatia própria, fora de SPEC de produto): novo helper `tests.unpinned_security_definer_functions()` — uma função `SECURITY DEFINER` em `public` sem `search_path` fixado, ou com um `search_path` que resolve por `"$user"`, passa a ser **falha de CI**. Estar na allowlist provava que a função foi **revisada**, nunca que era **segura de executar**: sem pin, nomes não qualificados resolvem pelo `search_path` de quem chama, e quem consegue criar um objeto num schema anterior no caminho decide o que a função executa — com os privilégios do dono. Provado pelo fixture negativo 004 (não pinada → detecta; pinada → limpa; `"$user"` → detecta de novo). Todas as 6 funções DEFINER existentes já cumpriam, então a mudança nasceu verde. pgTAP: **141 → 146 asserções**. |
| 2026-08-28 | v0.19: **SPEC-008 implementada e mergeada** em `main` via LEVEL 2 (**D-74**, agente §0.2 — aguarda ratificação; **D-75**, dependência `expo-notifications` aprovada por decisão humana). Lembretes locais: opt-in duplo, 3 intents puros, ≤2/dia, horizonte de 14 dias, id determinístico, reconciliação idempotente, texto de catálogo fixo sem PII. `notification_preferences` com RLS ON+FORCE, SELECT/INSERT/UPDATE próprios, **sem DELETE e sem RPC** (a linha não protege invariante do servidor). pgTAP: **146 → 161 asserções**. **Fase 7 fechada.** Auditoria `improve`: **2 BLOCKER** corrigidos — (1) o config plugin `expo-notifications` não estava em `app.json`, então o Android 13+ nunca declararia `POST_NOTIFICATIONS` e a feature seria inerte no aparelho, invisível para typecheck/testes/`expo export`; (2) `reconcile` agendava mesmo após a permissão ser revogada, contradizendo o EC2 da própria SPEC. **Nenhuma regra capilar — D-26 não acionada; gate de PUBLIC RELEASE inalterado.** |
| 2026-08-28 | v0.20: **D-74 RATIFICADA** por decisão humana (3 intents, `max_per_day` como constante, sem coluna/configuração no banco sem necessidade atual; SPEC-008 não reaberta). **SPEC-009 implementada e mergeada** em `main` via LEVEL 2 (**D-76**, agente §0.2 — aguarda ratificação). Progress v1: três fatos derivados do `TodayView`, **zero impacto em banco** (pgTAP inalterada em 161), sem score, porcentagem, tendência, gráfico ou streak. Barreira de teste com quatro padrões proibidos — porcentagem, score, tendência/melhora, claim de saúde — cada um com amostra que precisa casar. **Fase 8 parcialmente entregue** (falta SPEC-014 Reassessment). Auditoria `improve`: 3 IMPORTANT corrigidos — copy que dizia "cuidados que já passaram" era falsa para cuidado concluído adiantado; helper de teste que devolvia string vazia e faria as asserções passarem por vazio; `buildProgress` rodando fora do `useMemo`. **D-26 não acionada; gate de PUBLIC RELEASE inalterado.** |
| 2026-08-28 | v0.21: **SPEC-014 implementada e mergeada** em `main` via LEVEL 2 (**D-77**, agente §0.2 — aguarda ratificação). **Fase 8 fechada; o loop mensal está completo.** Reavaliação como fluxo de cliente sobre máquina que já existia: **zero migration, zero RPC, zero tela nova**; supersede só na confirmação; histórico nunca reescrito (D-69). Resolve o **OQ-2 da SPEC-009** — total vitalício de execuções efetivas, mostrado só quando acrescenta informação. Auditoria `improve`: 3 IMPORTANT corrigidos (a barreira de AC9 não exercitava a linha nova; fixture com campo errado; `exactOptionalPropertyTypes`) e **1 IMPORTANT reportado sem corrigir** — `packages/core/tsconfig.json` exclui `*.test.ts` do typecheck, então um teste do core pode chamar API com aridade errada, passar e afirmar sobre `undefined` (foi como o achado apareceu). Corrigir exige `tsconfig.test.json` próprio, porque `"types": []` é isolamento deliberado do pacote puro — **fatia própria**. **D-26 não acionada; gate de PUBLIC RELEASE inalterado.** |
| 2026-08-28 | v0.22: **Testes do core passam a ser type-checked** (fatia própria, sem SPEC de produto). `packages/core/tsconfig.json` excluía `*.test.ts`, então um teste podia chamar API com aridade errada, passar e afirmar sobre `undefined` — foi assim que o achado apareceu na SPEC-014. A correção **não** foi remover o `exclude`: `"types": []` é isolamento deliberado do pacote puro (build `emitDeclarationOnly`), então entrou um `tsconfig.test.json` separado com `vitest/globals`, `noEmit` e a mesma rigidez, ligado ao script `typecheck`. Verificado por quebra deliberada: uma chamada com um argumento a menos agora falha com `TS2554`. Nasceu verde. |
